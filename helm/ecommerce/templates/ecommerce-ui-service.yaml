apiVersion: v1
kind: Service

metadata:
  name: ecommerce-ui-service
  namespace: {{ .Values.namespace }}

spec:
  selector:
    app: ecommerce-ui

  ports:
    - protocol: TCP
      port: 80
      targetPort: {{ .Values.ui.port }}

  type: ClusterIP